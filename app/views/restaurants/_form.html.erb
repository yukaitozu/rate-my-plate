<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address%>
  <%= f.input :rating%>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Add", class: "btn-add" %>
<% end %>
